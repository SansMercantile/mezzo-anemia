# Comprehensive Python Dependencies for Sans Mercantile AI Ecosystem

# --- Core Frameworks & Utilities ---
fastapi==0.111.0
uvicorn==0.29.0
starlette==0.37.2
httpx==0.27.0
pydantic==2.7.4
pydantic-settings==2.3.4
python-dotenv==1.0.1
psutil==5.9.8 # For system monitoring (Watchdog)
schedule==1.2.2 # For simple task scheduling (if not using Celery/Redis)
rich==13.7.1 # For rich terminal output (if used for logging/CLI)
requests==2.32.4 # General HTTP requests (used by NewsAPIClient)
tqdm==4.67.1 # For progress bars (if used in scripts)
tabulate==0.9.0 # For pretty-printing tables (if used in scripts)
click==8.1.7 # Command Line Interface creation (if used)
# Note: 'uuid' is a standard library module, not a pip requirement.
# Note: 're', 'json', 'os', 'logging', 'asyncio', 'datetime', 'enum', 'hashlib', 'subprocess', 'random', 'math', 'collections' are built-in or standard library modules.

# --- Data Processing & Analysis ---
numpy==1.26.4
pandas==2.3.0
scipy==1.15.2
matplotlib==3.10.3 # For plotting/visualization (e.g., ESG Chart, AnalyticsEngine tests)
TA-Lib==0.4.32 # For technical analysis indicators (Python wrapper, C library dependency is handled in Dockerfile)
scikit-learn==1.7.0 # For ML models
statsmodels==0.14.4 # For statistical modeling
pyarrow==20.0.0 # For Feather file format (FeatureStore)
plotly==5.22.0 # For interactive plots (if used in backend for visualization)

# --- Google Cloud Client Libraries ---
google-cloud-storage==2.10.0 # For GCS operations (CloudOps, ZKVerifier)
google-cloud-secret-manager==2.19.0 # For Secret Manager access (CloudOps, GitOps)
google-cloud-pubsub==2.22.0 # For Pub/Sub messaging (MessageBrokerInterface, IssueResponder)
google-cloud-aiplatform==1.60.0 # For Vertex AI LLM interaction (AI-Ops)
google-cloud-logging==3.8.0 # For centralized logging (main.py)
google-api-core==2.19.0 # Core dependency for many GCP libraries
grpcio==1.64.1 # Core dependency for many GCP libraries

# --- Google API Clients (Specific) ---
google-api-python-client==2.131.0 # For interacting with Gmail API (EmailReader)
google-auth-oauthlib==1.2.0 # For OAuth 2.0 authentication (EmailReader)
google-auth-httplib2==0.2.0 # For HTTP client library (EmailReader)

# --- Database / ORM ---
sqlalchemy==2.0.41 # ORM for PostgreSQL
psycopg2-binary==2.9.9 # PostgreSQL adapter for Python

# --- Financial APIs & Brokers ---
# alpaca-py>=1.0.1,<2.0.0 # Alpaca API client (AlpacaAdapter)
python-binance==1.0.19 # Binance API client (BinanceAdapter)
python-deriv-api==0.1.6 # Deriv API client (DerivAdapter) - Pinned to a version compatible with websockets>=10.0.0
# Note: MT5 adapter is referenced, but its specific library (MetaTrader5) is not in provided Dockerfile/environment.yml.
# yfinance is installed in MPETI Dockerfile.
websockets==10.3 # For WebSocket connections (WebSocketManager)
feedparser==6.0.11 # For parsing RSS feeds (RSSRetriever)
# gdeltdoc is used by GlobalNewsIngestor, ensure it's installed if not via pip.
# The previous Dockerfile.txt listed `gdeltdoc` and `bs4`. `bs4` is included as `beautifulsoup4`.
beautifulsoup4==4.12.3 # For HTML parsing in web scrapers (TradingViewRetriever)
lxml==5.2.2 # For BeautifulSoup's parser

# --- ZKP (Zero-Knowledge Proofs) Integration ---
liboqs-python==0.14.0 # For Post-Quantum Cryptography (PQCEncryption)
web3==6.19.0 # For Ethereum blockchain interaction (BlockchainLogger)

# --- Zipline-related Libraries (from environment.yml in Dockerfile) ---
zipline-reloaded==3.1
empyrical-reloaded==0.5.12
exchange-calendars==4.10.1
bcolz-zipline==1.13.0
ffn==1.1.2

# --- Other identified dependencies ---
# Ensure they are truly needed or remove for leaner image if possible.
shellingham==1.5.4
typer==0.16.0
typing-extensions==4.14.1
sniffio==1.3.1
sortedcontainers==2.4.0
soupsieve==2.7
tables==3.10.2
toolz==1.0.0
ujson==5.10.0
uritemplate==4.2.0
urllib3==2.5.0
uvloop==0.21.0
watchfiles==1.1.0
joblib==1.5.1
kiwisolver==1.4.8
korean_lunar_calendar==0.3.1
lru-dict>=1.1.6,<1.3.0
markdown-it-py==3.0.0
mdurl==0.1.2
msgpack==1.1.1
multipledispatch==1.0.0
multitasking==0.0.11
ndindex==1.10.0
networkx==3.5
numexpr==2.11.0
oauthlib==3.3.1
opentelemetry-api==1.35.0
opentelemetry-sdk==1.35.0
opentelemetry-semantic-conventions==0.56b0
orjson==3.11.0
packaging==25.0
patsy==1.0.1
peewee==3.17.3
pillow==11.3.0
platformdirs==4.3.8
pygments==2.19.2
pyluach==2.2.0
pyparsing==3.2.3
python-dateutil==2.9.0.post0
python-interface==1.6.1
pytz==2025.2
pyyaml==6.0.2
requests-oauthlib==2.0.0
rich-toolkit==0.14.8
rsa==4.9.1
setuptools==70.0.0
wheel==0.43.0
pyasn1==0.6.1
pyasn1-modules==0.4.2
deepproblog==2.0.6
torch==2.0.1
tensorflow==2.19.0
